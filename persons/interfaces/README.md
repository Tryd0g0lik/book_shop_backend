 
 
## **This file contains the list below**:
 - [UsersPydantic](interface_persons.py) it is not only for a typing (it when we are using [Users model](../models/models_persons.py)) but also to work with the User's data, dictionary and json  
 - [EmailString](interface_emailStr.py) This is typing & validation of the email line from pidantic but also added a new property. This is a method when you want case replace a sub-text. 
 - [PersonServiceInitialize](interface_dataservice.py) it is for a typing.
